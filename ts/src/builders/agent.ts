// Auto-generated by the adk-fluent codegen pipeline.
// Do not edit by hand — re-run `just ts-generate`.

import { BuilderBase } from "../core/builder-base.js";

/**
 * Base class for all agents in Agent Development Kit.
 */
export class BaseAgent extends BuilderBase {
  constructor(name: string) {
    super(name);
  }

  /**
   * Set agent description (metadata for transfer routing and topology display — NOT sent to the LLM as instruction). Always set this on sub-agents so the coordinator LLM can pick the right specialist.
   */
  describe(value: string): this {
    return this._setConfig("description", value);
  }

  /**
   * Append callback(s) to `after_agent_callback`. Multiple calls accumulate.
   */
  afterAgent(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("after_agent_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `after_agent_callback` only if condition is True.
   */
  afterAgentIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("after_agent_callback", fn);
    }
    return this;
  }

  /**
   * Append callback(s) to `before_agent_callback`. Multiple calls accumulate.
   */
  beforeAgent(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("before_agent_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `before_agent_callback` only if condition is True.
   */
  beforeAgentIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("before_agent_callback", fn);
    }
    return this;
  }

  /**
   * Set the ``sub_agents`` field.
   */
  subAgents(value: unknown[]): this {
    return this._setConfig("sub_agents", value);
  }

  /**
   * Append to ``sub_agents`` (lazy — built at .build() time).
   */
  subAgent(value: unknown): this {
    return this._addToList("sub_agents", value);
  }

  /**
   * Base class for all agents in Agent Development Kit. Resolve into a native ADK _ADK_BaseAgent.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("BaseAgent");
  }
}

/**
 * LLM-based Agent.
 */
export class Agent extends BuilderBase {
  constructor(name: string, model: string | undefined = undefined) {
    super(name);
    if (model !== undefined) {
      this._config.set("model", model);
    }
  }

  /**
   * Set agent description (metadata for transfer routing and topology display — NOT sent to the LLM as instruction). Always set this on sub-agents so the coordinator LLM can pick the right specialist.
   */
  describe(value: string): this {
    return this._setConfig("description", value);
  }

  /**
   * Set instruction shared by ALL agents in a workflow. Only meaningful on the root agent. Prepended to every agent's system prompt.
   */
  globalInstruct(value: unknown): this {
    return this._setConfig("global_instruction", value);
  }

  /**
   * Set cached instruction. When set, ``.instruct()`` text moves from system to user content, enabling context caching. Use for large, stable prompt sections that rarely change.
   */
  static_(value: unknown): this {
    return this._setConfig("static_instruction", value);
  }

  /**
   * Deprecated: use ``.agent_tool()`` instead.
   */
  delegate(value: unknown): this {
    console.warn(".delegate() is deprecated, use .agent_tool() instead");
    return this._setConfig("_delegate", value);
  }

  /**
   * Deprecated: use ``.guard()`` instead.
   */
  guardrail(value: unknown): this {
    console.warn(".guardrail() is deprecated, use .guard() instead");
    return this._setConfig("_guardrail", value);
  }

  /**
   * Deprecated: use ``.context()`` instead.
   */
  history(value: unknown): this {
    console.warn(".history() is deprecated, use .context() instead");
    return this._setConfig("include_contents", value);
  }

  /**
   * Deprecated: use ``.context()`` instead.
   */
  includeHistory(value: unknown): this {
    console.warn(".include_history() is deprecated, use .context() instead");
    return this._setConfig("include_contents", value);
  }

  /**
   * Deprecated: use ``.prepend()`` instead.
   */
  injectContext(value: unknown): this {
    console.warn(".inject_context() is deprecated, use .prepend() instead");
    return this._setConfig("_inject_context", value);
  }

  /**
   * Deprecated: use ``.accepts()`` instead.
   */
  inputSchema(value: unknown): this {
    console.warn(".input_schema() is deprecated, use .accepts() instead");
    return this._setConfig("input_schema", value);
  }

  /**
   * Deprecated: use ``.writes()`` instead.
   */
  outputKey(value: string | undefined): this {
    console.warn(".output_key() is deprecated, use .writes() instead");
    return this._setConfig("output_key", value);
  }

  /**
   * Deprecated: use ``.returns()`` instead.
   */
  outputSchema(value: unknown): this {
    console.warn(".output_schema() is deprecated, use .returns() instead");
    return this._setConfig("output_schema", value);
  }

  /**
   * Deprecated: use ``.writes()`` instead.
   */
  outputs(value: string | undefined): this {
    console.warn(".outputs() is deprecated, use .writes() instead");
    return this._setConfig("output_key", value);
  }

  /**
   * Deprecated: use ``.loop_while()`` instead.
   */
  retryIf(value: unknown): this {
    console.warn(".retry_if() is deprecated, use .loop_while() instead");
    return this._setConfig("_retry_if", value);
  }

  /**
   * Deprecated: use ``.writes()`` instead.
   */
  saveAs(value: string | undefined): this {
    console.warn(".save_as() is deprecated, use .writes() instead");
    return this._setConfig("output_key", value);
  }

  /**
   * Deprecated: use ``.static()`` instead.
   */
  staticInstruct(value: unknown): this {
    console.warn(".static_instruct() is deprecated, use .static() instead");
    return this._setConfig("static_instruction", value);
  }

  /**
   * Append callback(s) to `after_agent_callback`. Multiple calls accumulate.
   */
  afterAgent(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("after_agent_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `after_agent_callback` only if condition is True.
   */
  afterAgentIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("after_agent_callback", fn);
    }
    return this;
  }

  /**
   * Append callback(s) to `after_model_callback`. Multiple calls accumulate.
   */
  afterModel(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("after_model_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `after_model_callback` only if condition is True.
   */
  afterModelIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("after_model_callback", fn);
    }
    return this;
  }

  /**
   * Append callback(s) to `after_tool_callback`. Multiple calls accumulate.
   */
  afterTool(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("after_tool_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `after_tool_callback` only if condition is True.
   */
  afterToolIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("after_tool_callback", fn);
    }
    return this;
  }

  /**
   * Append callback(s) to `before_agent_callback`. Multiple calls accumulate.
   */
  beforeAgent(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("before_agent_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `before_agent_callback` only if condition is True.
   */
  beforeAgentIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("before_agent_callback", fn);
    }
    return this;
  }

  /**
   * Append callback(s) to `before_model_callback`. Multiple calls accumulate.
   */
  beforeModel(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("before_model_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `before_model_callback` only if condition is True.
   */
  beforeModelIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("before_model_callback", fn);
    }
    return this;
  }

  /**
   * Append callback(s) to `before_tool_callback`. Multiple calls accumulate.
   */
  beforeTool(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("before_tool_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `before_tool_callback` only if condition is True.
   */
  beforeToolIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("before_tool_callback", fn);
    }
    return this;
  }

  /**
   * Append callback(s) to `on_model_error_callback`. Multiple calls accumulate.
   */
  onModelError(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("on_model_error_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `on_model_error_callback` only if condition is True.
   */
  onModelErrorIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("on_model_error_callback", fn);
    }
    return this;
  }

  /**
   * Append callback(s) to `on_tool_error_callback`. Multiple calls accumulate.
   */
  onToolError(...fns: unknown[]): this {
    let next: this = this;
    for (const fn of fns) {
      next = next._addCallback("on_tool_error_callback", fn);
    }
    return next;
  }

  /**
   * Append callback to `on_tool_error_callback` only if condition is True.
   */
  onToolErrorIf(condition: boolean, fn: (...args: unknown[]) => unknown): this {
    if (condition) {
      return this._addCallback("on_tool_error_callback", fn);
    }
    return this;
  }

  /**
   * Set the ``sub_agents`` field.
   */
  subAgents(value: unknown[]): this {
    return this._setConfig("sub_agents", value);
  }

  /**
   * Set the ``model`` field.
   */
  model(value: string | unknown): this {
    return this._setConfig("model", value);
  }

  /**
   * Set the ``generate_content_config`` field.
   */
  generateContentConfig(value: unknown | undefined): this {
    return this._setConfig("generate_content_config", value);
  }

  /**
   * Prevent this agent from transferring control back to its parent. Also forces an auto-handoff back to parent on the next turn. Equivalent to ``.stay()``. See also ``.isolate()``.
   */
  disallowTransferToParent(value: boolean): this {
    return this._setConfig("disallow_transfer_to_parent", value);
  }

  /**
   * Prevent this agent from transferring control to sibling agents. Equivalent to ``.no_peers()``. See also ``.isolate()``.
   */
  disallowTransferToPeers(value: boolean): this {
    return this._setConfig("disallow_transfer_to_peers", value);
  }

  /**
   * Set the ``planner`` field.
   */
  planner(value: unknown | undefined): this {
    return this._setConfig("planner", value);
  }

  /**
   * Set the ``code_executor`` field.
   */
  codeExecutor(value: unknown | undefined): this {
    return this._setConfig("code_executor", value);
  }

  /**
   * Append to ``sub_agents`` (lazy — built at .build() time).
   */
  subAgent(value: unknown): this {
    return this._addToList("sub_agents", value);
  }

  /**
   * Add a single tool (appends). Wraps plain callables in FunctionTool when require_confirmation=True.
   */
  tool(fn_or_tool: unknown, _require_confirmation: boolean = false): this {
    return this._addToList("tools", fn_or_tool);
  }

  /**
   * Set tools. Accepts a list, a TComposite chain (T.fn(x) | T.fn(y)), or a single tool/toolset.
   */
  tools(value: unknown): this {
    return this._addToList("tools", value);
  }

  /**
   * Attach A2UI surface for rich UI output. Declarative: .ui(UI.form(...)). LLM-guided: .ui(UI.auto()). Component tree: .ui(UI.column(UI.text('Hi'), UI.button('Go', action='go'))).
   */
  ui(spec: unknown): this {
    return this._setConfig("_ui_spec", spec);
  }

  /**
   * Add an output validation guard. Accepts a G composite (G.pii() | G.length(max=500)) or a plain callable. Guards run as after_model callbacks and validate/transform the LLM response before it is returned.
   */
  guard(value: unknown): this {
    let next: this = this._addCallback("before_model_callback", value);
    next = next._addCallback("after_model_callback", value);
    return next;
  }

  /**
   * Set the main instruction / system prompt — what the LLM is told to do. Accepts plain text, a callable, or a P module composition (P.role() + P.task()). Raises TypeError if passed a CTransform (use .context() instead).
   */
  instruct(value: unknown): this {
    return this._setConfig("instruction", value);
  }

  /**
   * Declare what conversation context this agent should see. Accepts a C module transform (C.none(), C.user_only(), C.from_state(), etc.).
   */
  context(spec: unknown): this {
    return this._setConfig("_context_spec", spec);
  }

  /**
   * Force this agent's events to be user-facing (override topology inference).
   */
  show(): this {
    return this._setConfig("_visibility", "show");
  }

  /**
   * Force this agent's events to be internal (override topology inference).
   */
  hide(): this {
    return this._setConfig("_visibility", "hide");
  }

  /**
   * Add memory tools to this agent. Modes: 'preload', 'on_demand', 'both'.
   */
  memory(mode: string = 'preload'): this {
    return this._setConfig("_memory", mode);
  }

  /**
   * Auto-save session to memory after each agent run.
   */
  memoryAutoSave(): this {
    return this._setConfig("_memory_auto_save", true);
  }

  /**
   * Wrap another agent as a callable AgentTool and add it to this agent's tools. The parent LLM invokes the child like any other tool, stays in control, and receives the response. Compare with .sub_agent() which fully transfers control to the child.
   */
  agentTool(agent: unknown): this {
    return this._addToList("tools", agent);
  }

  /**
   * Prevent this agent from transferring to parent or peers. Sets both disallow_transfer_to_parent and disallow_transfer_to_peers to True. Use for specialist agents that should complete their task and return.
   */
  isolate(): this {
    let next: this = this._setConfig("disallow_transfer_to_parent", true);
    next = next._setConfig("disallow_transfer_to_peers", true);
    return next;
  }

  /**
   * Prevent transfer to parent only (can still transfer to sibling peers). Equivalent to .disallow_transfer_to_parent(True). Use for agents in peer-to-peer handoff chains where the coordinator should not regain control mid-sequence.
   */
  stay(): this {
    return this._setConfig("disallow_transfer_to_parent", true);
  }

  /**
   * Prevent this agent from transferring to sibling agents. The agent can still return to its parent.
   */
  noPeers(): this {
    return this._setConfig("disallow_transfer_to_peers", true);
  }

  /**
   * Attach artifact operations (A.publish, A.snapshot, etc.) that fire after this agent completes.
   */
  artifacts(...transforms: unknown[]): this {
    let next: this = this;
    for (const item of transforms) {
      next = next._addToList("artifacts", item);
    }
    return next;
  }

  /**
   * Attach a ToolSchema declaring tool state dependencies.
   */
  toolSchema(schema: unknown): this {
    return this._setConfig("_tool_schema", schema);
  }

  /**
   * Attach a CallbackSchema declaring callback state dependencies.
   */
  callbackSchema(schema: unknown): this {
    return this._setConfig("_callback_schema", schema);
  }

  /**
   * Attach a PromptSchema declaring prompt state dependencies.
   */
  promptSchema(schema: unknown): this {
    return this._setConfig("_prompt_schema", schema);
  }

  /**
   * Attach an ArtifactSchema declaring artifact dependencies.
   */
  artifactSchema(schema: unknown): this {
    return this._setConfig("_artifact_schema", schema);
  }

  /**
   * Declare an A2A skill for this agent's AgentCard. Skills are metadata consumed by ``A2AServer`` during card generation. They have no effect on local agent execution. If no skills are declared, ``A2AServer`` auto-infers them from the agent's tools and sub-agents.
   */
  skill(skill_id: string, _name: string, _description: string = '', _tags: unknown = undefined, _examples: unknown = undefined, _input_modes: unknown = undefined, _output_modes: unknown = undefined): this {
    return this._addToList("skills", skill_id);
  }

  /**
   * LLM-based Agent. Resolve into a native ADK LlmAgent.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("LlmAgent");
  }
}
