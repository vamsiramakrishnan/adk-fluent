// Auto-generated by the adk-fluent codegen pipeline.
// Do not edit by hand — re-run `just ts-generate`.

import { BuilderBase } from "../core/builder-base.js";

/**
 * Abstract base class for all planners.
 */
export class BasePlanner extends BuilderBase {
  constructor(args: string, kwargs: string) {
    super(args);
    this._config.set("kwargs", kwargs);
  }

  /**
   * Abstract base class for all planners. Resolve into a native ADK _ADK_BasePlanner.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("BasePlanner");
  }
}

/**
 * The built-in planner that uses model's built-in thinking features.
 */
export class BuiltInPlanner extends BuilderBase {
  constructor(thinking_config: string) {
    super(thinking_config);
  }

  /**
   * The built-in planner that uses model's built-in thinking features. Resolve into a native ADK _ADK_BuiltInPlanner.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("BuiltInPlanner");
  }
}

/**
 * Plan-Re-Act planner that constrains the LLM response to generate a plan before any action/observation.
 */
export class PlanReActPlanner extends BuilderBase {
  constructor(args: string, kwargs: string) {
    super(args);
    this._config.set("kwargs", kwargs);
  }

  /**
   * Plan-Re-Act planner that constrains the LLM response to generate a plan before any action/observation. Resolve into a native ADK _ADK_PlanReActPlanner.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("PlanReActPlanner");
  }
}
