// Auto-generated by the adk-fluent codegen pipeline.
// Do not edit by hand — re-run `just ts-generate`.

import { BuilderBase } from "../core/builder-base.js";

/**
 * An AgentExecutor that runs an ADK Agent against an A2A request and.
 */
export class A2aAgentExecutor extends BuilderBase {
  constructor(runner: string) {
    super(runner);
  }

  /**
   * Set the ``config`` field.
   */
  config(value: unknown | undefined): this {
    return this._setConfig("config", value);
  }

  /**
   * An AgentExecutor that runs an ADK Agent against an A2A request and. Resolve into a native ADK _ADK_A2aAgentExecutor.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("A2aAgentExecutor");
  }
}

/**
 * A code executor that uses Agent Engine Code Execution Sandbox to execute code.
 */
export class AgentEngineSandboxCodeExecutor extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``optimize_data_file`` field.
   */
  optimizeDataFile(value: boolean): this {
    return this._setConfig("optimize_data_file", value);
  }

  /**
   * Set the ``stateful`` field.
   */
  stateful(value: boolean): this {
    return this._setConfig("stateful", value);
  }

  /**
   * Set the ``error_retry_attempts`` field.
   */
  errorRetryAttempts(value: number): this {
    return this._setConfig("error_retry_attempts", value);
  }

  /**
   * Set the ``code_block_delimiters`` field.
   */
  codeBlockDelimiters(value: unknown): this {
    return this._setConfig("code_block_delimiters", value);
  }

  /**
   * Set the ``execution_result_delimiters`` field.
   */
  executionResultDelimiters(value: unknown[]): this {
    return this._setConfig("execution_result_delimiters", value);
  }

  /**
   * Set the ``sandbox_resource_name`` field.
   */
  sandboxResourceName(value: string): this {
    return this._setConfig("sandbox_resource_name", value);
  }

  /**
   * Append to ``code_block_delimiters`` (lazy — built at .build() time).
   */
  codeBlockDelimiter(value: unknown[]): this {
    return this._addToList("code_block_delimiters", value);
  }

  /**
   * A code executor that uses Agent Engine Code Execution Sandbox to execute code. Resolve into a native ADK _ADK_AgentEngineSandboxCodeExecutor.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("AgentEngineSandboxCodeExecutor");
  }
}

/**
 * Abstract base class for all code executors.
 */
export class BaseCodeExecutor extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``optimize_data_file`` field.
   */
  optimizeDataFile(value: boolean): this {
    return this._setConfig("optimize_data_file", value);
  }

  /**
   * Set the ``stateful`` field.
   */
  stateful(value: boolean): this {
    return this._setConfig("stateful", value);
  }

  /**
   * Set the ``error_retry_attempts`` field.
   */
  errorRetryAttempts(value: number): this {
    return this._setConfig("error_retry_attempts", value);
  }

  /**
   * Set the ``code_block_delimiters`` field.
   */
  codeBlockDelimiters(value: unknown): this {
    return this._setConfig("code_block_delimiters", value);
  }

  /**
   * Set the ``execution_result_delimiters`` field.
   */
  executionResultDelimiters(value: unknown[]): this {
    return this._setConfig("execution_result_delimiters", value);
  }

  /**
   * Append to ``code_block_delimiters`` (lazy — built at .build() time).
   */
  codeBlockDelimiter(value: unknown[]): this {
    return this._addToList("code_block_delimiters", value);
  }

  /**
   * Abstract base class for all code executors. Resolve into a native ADK _ADK_BaseCodeExecutor.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("BaseCodeExecutor");
  }
}

/**
 * A code executor that uses the Model's built-in code executor.
 */
export class BuiltInCodeExecutor extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``optimize_data_file`` field.
   */
  optimizeDataFile(value: boolean): this {
    return this._setConfig("optimize_data_file", value);
  }

  /**
   * Set the ``stateful`` field.
   */
  stateful(value: boolean): this {
    return this._setConfig("stateful", value);
  }

  /**
   * Set the ``error_retry_attempts`` field.
   */
  errorRetryAttempts(value: number): this {
    return this._setConfig("error_retry_attempts", value);
  }

  /**
   * Set the ``code_block_delimiters`` field.
   */
  codeBlockDelimiters(value: unknown): this {
    return this._setConfig("code_block_delimiters", value);
  }

  /**
   * Set the ``execution_result_delimiters`` field.
   */
  executionResultDelimiters(value: unknown[]): this {
    return this._setConfig("execution_result_delimiters", value);
  }

  /**
   * Append to ``code_block_delimiters`` (lazy — built at .build() time).
   */
  codeBlockDelimiter(value: unknown[]): this {
    return this._addToList("code_block_delimiters", value);
  }

  /**
   * A code executor that uses the Model's built-in code executor. Resolve into a native ADK _ADK_BuiltInCodeExecutor.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("BuiltInCodeExecutor");
  }
}

/**
 * A code executor that unsafely execute code in the current local context.
 */
export class UnsafeLocalCodeExecutor extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``optimize_data_file`` field.
   */
  optimizeDataFile(value: boolean): this {
    return this._setConfig("optimize_data_file", value);
  }

  /**
   * Set the ``stateful`` field.
   */
  stateful(value: boolean): this {
    return this._setConfig("stateful", value);
  }

  /**
   * Set the ``error_retry_attempts`` field.
   */
  errorRetryAttempts(value: number): this {
    return this._setConfig("error_retry_attempts", value);
  }

  /**
   * Set the ``code_block_delimiters`` field.
   */
  codeBlockDelimiters(value: unknown): this {
    return this._setConfig("code_block_delimiters", value);
  }

  /**
   * Set the ``execution_result_delimiters`` field.
   */
  executionResultDelimiters(value: unknown[]): this {
    return this._setConfig("execution_result_delimiters", value);
  }

  /**
   * Append to ``code_block_delimiters`` (lazy — built at .build() time).
   */
  codeBlockDelimiter(value: unknown[]): this {
    return this._addToList("code_block_delimiters", value);
  }

  /**
   * A code executor that unsafely execute code in the current local context. Resolve into a native ADK _ADK_UnsafeLocalCodeExecutor.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("UnsafeLocalCodeExecutor");
  }
}

/**
 * A code executor that uses Vertex Code Interpreter Extension to execute code.
 */
export class VertexAiCodeExecutor extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``optimize_data_file`` field.
   */
  optimizeDataFile(value: unknown): this {
    return this._setConfig("optimize_data_file", value);
  }

  /**
   * Set the ``stateful`` field.
   */
  stateful(value: unknown): this {
    return this._setConfig("stateful", value);
  }

  /**
   * Set the ``error_retry_attempts`` field.
   */
  errorRetryAttempts(value: unknown): this {
    return this._setConfig("error_retry_attempts", value);
  }

  /**
   * Set the ``code_block_delimiters`` field.
   */
  codeBlockDelimiters(value: unknown): this {
    return this._setConfig("code_block_delimiters", value);
  }

  /**
   * Set the ``execution_result_delimiters`` field.
   */
  executionResultDelimiters(value: unknown): this {
    return this._setConfig("execution_result_delimiters", value);
  }

  /**
   * Set the ``resource_name`` field.
   */
  resourceName(value: unknown): this {
    return this._setConfig("resource_name", value);
  }

  /**
   * A code executor that uses Vertex Code Interpreter Extension to execute code. Resolve into a native ADK _ADK_VertexAiCodeExecutor.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("VertexAiCodeExecutor");
  }
}
