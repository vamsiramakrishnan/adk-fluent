// Auto-generated by the adk-fluent codegen pipeline.
// Do not edit by hand — re-run `just ts-generate`.

import { BuilderBase } from "../core/builder-base.js";

/**
 * Plugin for recording ADK agent interactions.
 */
export class RecordingsPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * Plugin for recording ADK agent interactions. Resolve into a native ADK _ADK_RecordingsPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("RecordingsPlugin");
  }
}

/**
 * Plugin for replaying ADK agent interactions from recordings.
 */
export class ReplayPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * Plugin for replaying ADK agent interactions from recordings. Resolve into a native ADK _ADK_ReplayPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("ReplayPlugin");
  }
}

/**
 * Base class for creating plugins.
 */
export class BasePlugin extends BuilderBase {
  constructor(name: string) {
    super(name);
  }

  /**
   * Base class for creating plugins. Resolve into a native ADK _ADK_BasePlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("BasePlugin");
  }
}

/**
 * BigQuery Agent Analytics Plugin (v2.0 using Write API).
 */
export class BigQueryAgentAnalyticsPlugin extends BuilderBase {
  constructor(project_id: string, dataset_id: string, kwargs: string) {
    super(project_id);
    this._config.set("dataset_id", dataset_id);
    this._config.set("kwargs", kwargs);
  }

  /**
   * Set the ``table_id`` field.
   */
  tableId(value: string | undefined): this {
    return this._setConfig("table_id", value);
  }

  /**
   * Set the ``config`` field.
   */
  config(value: unknown | undefined): this {
    return this._setConfig("config", value);
  }

  /**
   * Set the ``location`` field.
   */
  location(value: string): this {
    return this._setConfig("location", value);
  }

  /**
   * BigQuery Agent Analytics Plugin (v2.0 using Write API). Resolve into a native ADK _ADK_BigQueryAgentAnalyticsPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("BigQueryAgentAnalyticsPlugin");
  }
}

/**
 * A plugin that filters the LLM context to reduce its size.
 */
export class ContextFilterPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``num_invocations_to_keep`` field.
   */
  numInvocationsToKeep(value: number | undefined): this {
    return this._setConfig("num_invocations_to_keep", value);
  }

  /**
   * Set the ``custom_filter`` field.
   */
  customFilter(value: (...args: unknown[]) => unknown): this {
    return this._setConfig("custom_filter", value);
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * A plugin that filters the LLM context to reduce its size. Resolve into a native ADK _ADK_ContextFilterPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("ContextFilterPlugin");
  }
}

/**
 * A plugin that captures complete debug information to a file.
 */
export class DebugLoggingPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * Set the ``output_path`` field.
   */
  outputPath(value: string): this {
    return this._setConfig("output_path", value);
  }

  /**
   * Set the ``include_session_state`` field.
   */
  includeSessionState(value: boolean): this {
    return this._setConfig("include_session_state", value);
  }

  /**
   * Set the ``include_system_instruction`` field.
   */
  includeSystemInstruction(value: boolean): this {
    return this._setConfig("include_system_instruction", value);
  }

  /**
   * A plugin that captures complete debug information to a file. Resolve into a native ADK _ADK_DebugLoggingPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("DebugLoggingPlugin");
  }
}

/**
 * Plugin that provides global instructions functionality at the App level.
 */
export class GlobalInstructionPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``global_instruction`` field.
   */
  globalInstruction(value: string | unknown): this {
    return this._setConfig("global_instruction", value);
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * Plugin that provides global instructions functionality at the App level. Resolve into a native ADK _ADK_GlobalInstructionPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("GlobalInstructionPlugin");
  }
}

/**
 * A plugin that logs important information at each callback point.
 */
export class LoggingPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * A plugin that logs important information at each callback point. Resolve into a native ADK _ADK_LoggingPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("LoggingPlugin");
  }
}

/**
 * A plugin that modifies function tool responses to support returning list of parts directly.
 */
export class MultimodalToolResultsPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * A plugin that modifies function tool responses to support returning list of parts directly. Resolve into a native ADK _ADK_MultimodalToolResultsPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("MultimodalToolResultsPlugin");
  }
}

/**
 * Provides self-healing, concurrent-safe error recovery for tool failures.
 */
export class ReflectAndRetryToolPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * Set the ``max_retries`` field.
   */
  maxRetries(value: number): this {
    return this._setConfig("max_retries", value);
  }

  /**
   * Set the ``throw_exception_if_retry_exceeded`` field.
   */
  throwExceptionIfRetryExceeded(value: boolean): this {
    return this._setConfig("throw_exception_if_retry_exceeded", value);
  }

  /**
   * Set the ``tracking_scope`` field.
   */
  trackingScope(value: unknown): this {
    return this._setConfig("tracking_scope", value);
  }

  /**
   * Provides self-healing, concurrent-safe error recovery for tool failures. Resolve into a native ADK _ADK_ReflectAndRetryToolPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("ReflectAndRetryToolPlugin");
  }
}

/**
 * A plugin that saves files embedded in user messages as artifacts.
 */
export class SaveFilesAsArtifactsPlugin extends BuilderBase {
  constructor() {
    super("");
  }

  /**
   * Set the ``name`` field.
   */
  name_(value: string): this {
    return this._setConfig("name", value);
  }

  /**
   * A plugin that saves files embedded in user messages as artifacts. Resolve into a native ADK _ADK_SaveFilesAsArtifactsPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("SaveFilesAsArtifactsPlugin");
  }
}

/**
 * ADK Plugin for AgentSimulator.
 */
export class AgentSimulatorPlugin extends BuilderBase {
  constructor(simulator_engine: string) {
    super(simulator_engine);
  }

  /**
   * ADK Plugin for AgentSimulator. Resolve into a native ADK _ADK_AgentSimulatorPlugin.
   */
  build(): Record<string, unknown> {
    return this._buildConfig("AgentSimulatorPlugin");
  }
}
