"""Bundled A2UI protocol data (catalog + message schemas).

The JSON files in this package are copied from the canonical A2UI
specification and shipped with the wheel so the catalog provider works
fully offline.
"""
